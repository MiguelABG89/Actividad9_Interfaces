import { createContext } from "react";

const CardContext = createContext(null);
const ItemsContext = createContext(null)
const EventContext = createContext(null);

export {CardContext,EventContext,ItemsContext};
